/* eslint-disable prettier/prettier */
class ButtonLogConfig {
    mode = 0;
    frequency = 0;

    constructor(mode: number, frequency: number) {
        this.mode = mode;
        this.frequency = frequency;
    }
}
export default ButtonLogConfig;
